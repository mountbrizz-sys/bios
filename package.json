{
  "name": "bios",
  "version": "1.0.0",
  "private": true,
  "description": "Bios — personal productivity PWA (habits, calendar, deadlines, cloud sync)",
  "scripts": {
    "test": "for f in tests/test_*.js; do echo \"=== $f ===\"; node \"$f\" || exit 1; done; echo; echo ALL SUITES PASSED"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
